import 'package:flutter/material.dart';

class KelolaEdukasiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text(
          'Halaman Kelola Edukasi',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}